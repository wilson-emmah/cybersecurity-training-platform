"use client";
import Link from "next/link"; import {useState} from "react"; import {API} from "../../lib/api";
export default function Register(){const [msg,setMsg]=useState("");const [error,setError]=useState("");
async function submit(e){e.preventDefault();setMsg("");setError("");const f=new FormData(e.currentTarget);try{const r=await fetch(`${API}/auth/register/`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:f.get("username"),email:f.get("email"),password:f.get("password")})});const d=await r.json();if(!r.ok)throw new Error(Object.values(d).flat().join(" "));setMsg("Account created. You can now sign in.");e.currentTarget.reset()}catch(x){setError(x.message)}}return <main className="auth"><div className="authCard"><div className="brand">Cyber<span>Guard</span></div><p className="eyebrow">CREATE ACCOUNT</p><h1>Start learning</h1><form onSubmit={submit}><label>Username<input name="username" required minLength="3"/></label><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required minLength="8"/></label><button className="button full">Create Account</button></form>{msg&&<p className="success">{msg}</p>}{error&&<p className="error">{error}</p>}<p className="authFooter">Already registered? <Link href="/login">Sign in</Link></p></div></main>}
